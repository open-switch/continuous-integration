---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: opxhub/fly
inputs:
- name: src
run:
  path: bash
  args:
  - -ec
  - |
    for f in $(find src/pipelines/ -name '*.yaml'); do
      echo -n "$f "
      fly validate-pipeline -c "$f"
    done

